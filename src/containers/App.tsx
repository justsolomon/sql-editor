import Editor from './Editor';
import Layout from 'components/global/Layout';
import 'styles/main.scss';

const App = () => {
  return (
    <Layout>
      <Editor />
    </Layout>
  );
};

export default App;
